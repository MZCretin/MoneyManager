<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diandian.ycdyus.moneymanager" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.SpalshActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.BaseActivity" />
        <activity android:name=".ui.GuideActivity" />
        <activity android:name=".ui.MainActivity_" />
        <activity android:name=".ui.BudgetActivity_"/>
        <activity android:name=".ui.BudgetManagerActivity_"/>
        <activity
            android:name=".ui.NewRecordActivity_"
            android:label="@string/title_activity_new_record" >
        </activity>
    </application>

</manifest>
